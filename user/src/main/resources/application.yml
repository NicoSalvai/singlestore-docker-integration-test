server:
  port: 8085

spring:
  application:
    name: user
  datasource:
    username: 'root'
    url: jdbc:mysql://localhost:3306/playmaker
    password: '123321'
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
          format_sql: 'true'
      hibernate:
        ddl-auto: update
      show_sql: 'true'